diff --git a/chrome/android/java/src/org/chromium/chrome/browser/ntp/NewTabPage.java b/chrome/android/java/src/org/chromium/chrome/browser/ntp/NewTabPage.java
--- a/chrome/android/java/src/org/chromium/chrome/browser/ntp/NewTabPage.java
+++ b/chrome/android/java/src/org/chromium/chrome/browser/ntp/NewTabPage.java
@@ -1,7 +1,9 @@
         @Override
         public boolean isLocationBarShownInNtp() {
             if (mIsDestroyed) return false;
-            return isInSingleUrlBarMode() && !mNewTabPageCoordinator.urlFocusAnimationsDisabled();
+            // Kiwi keeps the real toolbar omnibox pinned at the top of a new tab instead of
+            // morphing it into Chromium's large in-page fake search box.
+            return false;
         }
 
         @Override
diff --git a/chrome/android/java/src/org/chromium/chrome/browser/ntp/NewTabPageCoordinator.java b/chrome/android/java/src/org/chromium/chrome/browser/ntp/NewTabPageCoordinator.java
--- a/chrome/android/java/src/org/chromium/chrome/browser/ntp/NewTabPageCoordinator.java
+++ b/chrome/android/java/src/org/chromium/chrome/browser/ntp/NewTabPageCoordinator.java
@@ -2,6 +2,10 @@
                         mProfile,
                         mBackPressManager);
         mModel.set(NewTabPageLayoutProperties.SEARCH_BOX_VIEW, mNtpSearchBox.getView());
+        // The real toolbar omnibox is always used on phones for the Kiwi layout. Keep the
+        // Chromium fakebox instantiated for the surrounding NTP plumbing, but remove it from
+        // layout so it does not reserve the oversized centered search-box space.
+        mNtpSearchBox.getView().setVisibility(View.GONE);
 
         updateSearchBoxTwoSideMargin();
         initializeLogoCoordinator();
diff --git a/chrome/android/java/src/org/chromium/chrome/browser/history/HistoryContentManager.java b/chrome/android/java/src/org/chromium/chrome/browser/history/HistoryContentManager.java
--- a/chrome/android/java/src/org/chromium/chrome/browser/history/HistoryContentManager.java
+++ b/chrome/android/java/src/org/chromium/chrome/browser/history/HistoryContentManager.java
@@ -2,11 +2,9 @@
         return HistoryManager.isAppSpecificHistoryEnabled() && mShowAppFilter;
     }
 
-    /** returns whether the info header will be available for user upon request. */
+    /** Kiwi omits Chromium's app/history informational banner above the history list. */
     boolean isInfoHeaderAvailable() {
-        // Info header becomes available when history was launched for app-specific mode
-        // or a set of conditions for privacy disclaimer are all met.
-        return launchedForApp() || hasPrivacyDisclaimers();
+        return false;
     }
 
     /** Called after a user clicks the privacy disclaimer link. */
diff --git a/chrome/browser/ui/android/omnibox/java/src/org/chromium/chrome/browser/omnibox/suggestions/SuggestionListViewBinder.java b/chrome/browser/ui/android/omnibox/java/src/org/chromium/chrome/browser/omnibox/suggestions/SuggestionListViewBinder.java
--- a/chrome/browser/ui/android/omnibox/java/src/org/chromium/chrome/browser/omnibox/suggestions/SuggestionListViewBinder.java
+++ b/chrome/browser/ui/android/omnibox/java/src/org/chromium/chrome/browser/omnibox/suggestions/SuggestionListViewBinder.java
@@ -1,10 +1,13 @@
         holder.dropdown.setBackgroundColor(backgroundColor);
 
+        // Kiwi leaves the current page visible behind the focused omnibox and darkens it with a
+        // translucent scrim. The dropdown itself remains opaque; only the unused phone area is
+        // translucent so the page can still be seen.
         if (model.get(SuggestionListProperties.IS_LARGE_SCREEN)
                 && layoutMode != FuseboxLayoutMode.SUGGESTIONS_POPOVER) {
             holder.container.setBackgroundColor(Color.TRANSPARENT);
         } else {
-            holder.container.setBackgroundColor(backgroundColor);
+            holder.container.setBackgroundColor(Color.argb(179, 0, 0, 0));
         }
     }
 
diff --git a/chrome/browser/ui/android/omnibox/java/src/org/chromium/chrome/browser/omnibox/suggestions/OmniboxSuggestionsContainer.java b/chrome/browser/ui/android/omnibox/java/src/org/chromium/chrome/browser/omnibox/suggestions/OmniboxSuggestionsContainer.java
--- a/chrome/browser/ui/android/omnibox/java/src/org/chromium/chrome/browser/omnibox/suggestions/OmniboxSuggestionsContainer.java
+++ b/chrome/browser/ui/android/omnibox/java/src/org/chromium/chrome/browser/omnibox/suggestions/OmniboxSuggestionsContainer.java
@@ -3,6 +3,7 @@
     private OmniboxSuggestionsDropdown mDropdown;
     private @Nullable OmniboxSuggestionsDropdownEmbedder mEmbedder;
     private @Nullable Callback<Integer> mHeightChangeListener;
+    private @Nullable Runnable mDismissOmniboxCallback;
     private OmniboxAlignment mOmniboxAlignment = OmniboxAlignment.UNSPECIFIED;
 
     private int mListViewMaxHeight;
@@ -54,13 +55,21 @@
     @Override
     @SuppressLint("ClickableViewAccessibility")
     public boolean onTouchEvent(MotionEvent event) {
-        // Swallow all touch events, especially if these were not consumed by the Dropdown.
-        // This ensures that touching the blank areas of the container does not dismiss the
-        // Omnibox.
-        // Making the container `clickable=true` achieves similar goal, but this consumes all
-        // activators, including keyboard <Enter> key.
+        // Kiwi treats the dimmed area below the actual suggestions as a cancel target. Taps on
+        // suggestion rows are still handled by the dropdown child itself.
+        if (event.getActionMasked() == MotionEvent.ACTION_UP
+                && mDismissOmniboxCallback != null
+                && (mDropdown.getVisibility() != View.VISIBLE
+                        || event.getY() >= mDropdown.getBottom())) {
+            mDismissOmniboxCallback.run();
+        }
         super.onTouchEvent(event);
         return true;
+    }
+
+    /** Sets the callback used when the user taps the dimmed area outside suggestion rows. */
+    public void setDismissOmniboxCallback(@Nullable Runnable callback) {
+        mDismissOmniboxCallback = callback;
     }
 
     /**
diff --git a/chrome/browser/ui/android/omnibox/java/src/org/chromium/chrome/browser/omnibox/suggestions/AutocompleteCoordinator.java b/chrome/browser/ui/android/omnibox/java/src/org/chromium/chrome/browser/omnibox/suggestions/AutocompleteCoordinator.java
--- a/chrome/browser/ui/android/omnibox/java/src/org/chromium/chrome/browser/omnibox/suggestions/AutocompleteCoordinator.java
+++ b/chrome/browser/ui/android/omnibox/java/src/org/chromium/chrome/browser/omnibox/suggestions/AutocompleteCoordinator.java
@@ -3,6 +3,7 @@
                 (holder) -> {
                     mContainer = holder.container;
                     mDropdown = holder.dropdown;
+                    mContainer.setDismissOmniboxCallback(delegate::clearOmniboxFocus);
                 });
         LazyConstructionPropertyMcp.create(
                 listModel,
