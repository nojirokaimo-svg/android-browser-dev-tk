diff --git a/components/autofill/core/common/autofill_prefs.cc b/components/autofill/core/common/autofill_prefs.cc
--- a/components/autofill/core/common/autofill_prefs.cc
+++ b/components/autofill/core/common/autofill_prefs.cc
@@ -141,7 +141,7 @@ void RegisterProfilePrefs(user_prefs::PrefRegistrySyncable* registry) {
 #endif
 
 #if BUILDFLAG(IS_ANDROID)
-  registry->RegisterBooleanPref(kAutofillUsingPlatformAutofill, true);
+  registry->RegisterBooleanPref(kAutofillUsingPlatformAutofill, false);
   registry->RegisterBooleanPref(kAutofillThirdPartyPasswordManagersAllowed,
                                 true);
   registry->RegisterStringPref(
diff --git a/components/password_manager/core/common/password_manager_pref_names.h b/components/password_manager/core/common/password_manager_pref_names.h
--- a/components/password_manager/core/common/password_manager_pref_names.h
+++ b/components/password_manager/core/common/password_manager_pref_names.h
@@ -273,5 +273,11 @@ inline constexpr char kClearingUndecryptablePasswords[] =
     "password_manager.clearing_undecryptable_passwords";
 
+// One-time downstream migration marker. Existing Android profiles created
+// while platform Autofill was the default need to be moved to the restored
+// local password backend once, without overriding later user choices.
+inline constexpr char kTitaniumLocalPasswordBackendMigrated[] =
+    "password_manager.titanium_local_backend_migrated";
+
 // Boolean pref indicating if passwords were migrated to OSCryptAsync. Two for
 // each store.
 inline constexpr char kProfileStoreMigratedToOSCryptAsync[] =
diff --git a/components/password_manager/core/browser/password_manager.cc b/components/password_manager/core/browser/password_manager.cc
--- a/components/password_manager/core/browser/password_manager.cc
+++ b/components/password_manager/core/browser/password_manager.cc
@@ -579,8 +579,10 @@ void PasswordManager::RegisterProfilePrefs(
       user_prefs::PrefRegistrySyncable::SYNCABLE_PREF);
   registry->RegisterBooleanPref(prefs::kPasswordsPrefWithNewLabelUsed, false);
+  registry->RegisterBooleanPref(
+      prefs::kTitaniumLocalPasswordBackendMigrated, false);
 #if BUILDFLAG(IS_ANDROID)
-  registry->RegisterBooleanPref(prefs::kOfferToSavePasswordsEnabledGMS, false);
-  registry->RegisterBooleanPref(prefs::kAutoSignInEnabledGMS, false);
+  registry->RegisterBooleanPref(prefs::kOfferToSavePasswordsEnabledGMS, true);
+  registry->RegisterBooleanPref(prefs::kAutoSignInEnabledGMS, true);
   registry->RegisterStringPref(prefs::kUPMErrorUIShownTimestamp, "0");
   registry->RegisterIntegerPref(
       prefs::kPasswordGenerationBottomSheetDismissCount, 0);
diff --git a/chrome/browser/prefs/browser_prefs.cc b/chrome/browser/prefs/browser_prefs.cc
--- a/chrome/browser/prefs/browser_prefs.cc
+++ b/chrome/browser/prefs/browser_prefs.cc
@@ -166,6 +166,8 @@
 #include "components/optimization_guide/core/optimization_guide_prefs.h"
 #include "components/page_info/core/merchant_trust_service.h"
 #include "components/password_manager/core/browser/password_manager.h"
+#include "components/password_manager/core/browser/password_manager_buildflags.h"
+#include "components/password_manager/core/common/password_manager_pref_names.h"
 #include "components/payments/core/payment_prefs.h"
 #include "components/performance_manager/public/user_tuning/prefs.h"
 #include "components/permissions/permission_hats_trigger_helper.h"
@@ -2512,6 +2514,28 @@ void MigrateObsoleteProfilePrefs(PrefService* profile_prefs,
 #endif  // BUILDFLAG(IS_CHROMEOS)
 
 #if BUILDFLAG(IS_ANDROID)
+#if BUILDFLAG(USE_LOGIN_DATABASE_AS_BACKEND)
+  // Titanium previously inherited Vanadium's platform-Autofill default. Move
+  // existing profiles to the restored local LoginDatabase backend exactly
+  // once so save/update prompts and persisted filling are enabled. Managed
+  // values remain authoritative, and after this marker is set the user's
+  // explicit Autofill choice is never overwritten on later starts.
+  if (!profile_prefs->GetBoolean(
+          password_manager::prefs::kTitaniumLocalPasswordBackendMigrated)) {
+    if (!profile_prefs->IsManagedPreference(
+            password_manager::prefs::kCredentialsEnableService)) {
+      profile_prefs->SetBoolean(
+          password_manager::prefs::kCredentialsEnableService, true);
+    }
+    if (!profile_prefs->IsManagedPreference(
+            autofill::prefs::kAutofillUsingPlatformAutofill)) {
+      profile_prefs->SetBoolean(
+          autofill::prefs::kAutofillUsingPlatformAutofill, false);
+    }
+    profile_prefs->SetBoolean(
+        password_manager::prefs::kTitaniumLocalPasswordBackendMigrated, true);
+  }
+#endif  // BUILDFLAG(USE_LOGIN_DATABASE_AS_BACKEND)
   // Added 09/2025.
   profile_prefs->ClearPref(kObsoleteUpmUnmigratedPasswordsExported);
   profile_prefs->ClearPref(kObsoletePasswordsUseUPMLocalAndSeparateStores);
