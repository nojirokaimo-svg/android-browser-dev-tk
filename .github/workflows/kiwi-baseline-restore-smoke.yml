name: Smoke-test durable Titanium-Kiwi baseline restore

# One-off disaster-recovery proof completed successfully on 2026-09-18.
# Kept inert for audit/reference; it will not run on push.
on:
  workflow_dispatch:

permissions:
  contents: read
  actions: read

jobs:
  archived-proof:
    runs-on: ubuntu-latest
    steps:
      - run: echo "One-off Release -> Actions Cache restore proof already completed successfully."
